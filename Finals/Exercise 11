Example 4
#include ‹iostream> 
#include <cstdlib>
using namespace std;

int main()
{
  int* ptr = (int*) malloc(sizeof(int)): // Dynamically allocate memory
  if (ptr != nullptr) l
  *ptr = 10;
  cout « "Value:" « *ptr «< endi;
  free(ptr); // Free allocated memory
  }
  return 0;
}
